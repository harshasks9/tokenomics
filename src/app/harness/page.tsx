import Site from "@/components/harness/Site";

/**
 * HARNESS CHOICE — a single client-rendered route.
 * All facts render from the typed research snapshot in src/lib/harness/;
 * components carry presentation and interaction only.
 */
export default function HarnessPage() {
  return <Site />;
}
