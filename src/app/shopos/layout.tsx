import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "ShopOS Tokenomics Showcase - Tiered Model Economics",
  description:
    "Retail model-routing economics across development, shopping assistance, visual search, and merchandising agents.",
};

export default function ShopOSLayout({ children }: { children: React.ReactNode }) {
  return children;
}
